#Assinale correto ou incorreto. Considere o código abaixo e assinale o que cada conjunto de variáveis imprime (cada conjunto separado por uma cor diferente).
if(aparelho == 'tv'):
     if(marca == 'sony'):
        if(tamanho == 48):
            print('Equipamento:', aparelho, marca, tamanho)
        else: 
           tamanho = 50
     elif(marca == 'samsung'):
          print('Equipamento:', aparelho, marca, tamanho)
     else:
           print('Equipamento:', aparelho, marca, tamanho)